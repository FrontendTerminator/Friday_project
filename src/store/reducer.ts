type ReducerStateType = {}
type ReducerActionType = {}
let initialState: ReducerStateType = {}

export const reducer = (state: ReducerStateType = initialState, action: ReducerActionType): ReducerStateType => {
    switch (action) {
        default:
            return state
    }
}